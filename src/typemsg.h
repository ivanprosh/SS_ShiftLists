#ifndef TYPEMSG_H
#define TYPEMSG_H

namespace EventLogScope {
    enum severity_level
    {
        normal,
        notification,
        warning,
        error,
        critical
    };
}

#endif // TYPEMSG_H
